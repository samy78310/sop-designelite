# Vercel Postgres
POSTGRES_URL=

# Next Auth v5 (uses AUTH_SECRET, not NEXTAUTH_SECRET)
AUTH_SECRET=your-secret-here-generate-with-openssl-rand-base64-32
NEXTAUTH_URL=http://localhost:3000

# Reader password (shared team password for /docs)
READER_PASSWORD=bipbip2

# Vercel Blob (for image uploads in admin editor)
BLOB_READ_WRITE_TOKEN=
